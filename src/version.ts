/**
 * Package version, inlined so the bundle stays dependency-free and platform-neutral
 * (no `require('../package.json')`). Kept in sync by test/version.test.ts.
 */
export const VERSION = '0.1.0';
